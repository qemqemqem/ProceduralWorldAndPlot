using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace CSD{

	public class PlayerUI : MonoBehaviour {
		public HumanPlayer player;
		public UIActions actions;//replace with UI actions
		public Transform uiFocus;
		//track focus and clicks for the specfied player only
		//provide interface for spawning and populating various menus

		// Use this for initialization
		void Start () {
			
		}
		
		// Update is called once per frame
		void Update () {
			
		}
	}

}